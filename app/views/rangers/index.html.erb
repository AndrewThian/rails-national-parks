<h1>Rangers</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @ranger.each do |ranger| %>
      <tr>
        <td><%= ranger.name %></td>
        <td><%= link_to 'Show', ranger %></td>
        <td><%= link_to 'Edit', edit_ranger_path(ranger) %></td>
        <td><%= link_to 'Destroy', ranger, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<!-- path wrong -->
<%= link_to 'New Ranger', new_ranger_path %>
